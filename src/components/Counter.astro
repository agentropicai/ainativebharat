---
---

<section id="counter" class="flex flex-col items-center justify-center min-h-screen px-6 text-center">
  <p class="text-subtle text-sm uppercase tracking-widest mb-8 font-medium">AI Native Bharat</p>

  <h1 id="count-number" class="font-mono text-7xl md:text-9xl font-bold text-white tabular-nums leading-none">
    &ndash;
  </h1>

  <p class="text-2xl md:text-3xl text-subtle mt-4 font-heading">
    Indians are <span class="text-saffron font-semibold">AI Native</span>.
  </p>

  <p id="remaining-text" class="text-lg text-muted mt-2 font-mono">
  </p>

  <p class="text-subtle mt-8 text-base max-w-md leading-relaxed">
    Each one enables 3 more.
  </p>

  <a
    href="#definition"
    class="mt-12 text-saffron hover:text-saffron-light transition-colors text-sm flex flex-col items-center gap-2"
  >
    <svg class="w-4 h-4 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3" />
    </svg>
  </a>
</section>

<script is:inline>
  (async function() {
    var url = 'https://vovvzpyjrfrvogftvrth.supabase.co/rest/v1/founders?select=*&verified=eq.true';
    var key = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InZvdnZ6cHlqcmZydm9nZnR2cnRoIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzA5ODg2OTksImV4cCI6MjA4NjU2NDY5OX0.JfsgPqCcY5XUYqAN2Vo9RA-p-4-Yht8qd_j3gT1MK1Q';

    try {
      var res = await fetch(url, {
        headers: { 'apikey': key, 'Authorization': 'Bearer ' + key }
      });
      var data = await res.json();
      window.__founders = data;
      var count = data.length;
      var remaining = (1400000000 - count).toLocaleString('en-IN');

      document.getElementById('count-number').textContent = count;
      document.getElementById('remaining-text').textContent = remaining + ' to go.';
    } catch (e) {
      document.getElementById('count-number').textContent = '2';
      document.getElementById('remaining-text').textContent = '1,39,99,99,998 to go.';
    }
  })();
</script>
